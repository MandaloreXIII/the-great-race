function race() {
  var winner = null;
  do {
    // Sets the speeds of the racers.
    var racer1_speed = Math.floor(Math.random()*(3)) + 4;
    var racer2_speed = Math.floor(Math.random()*(3)) + 4;
    // INSERT THE ITERATION OF CARS ON SCREEN

    //Check if car passed finish line

  }
   while (winner = null);
}
